[tool.poetry]
name = "pyltk"
version = "0.1.0"
description = "NLTk toolkit using python"
authors = ["Sleak07 <trisky650@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
